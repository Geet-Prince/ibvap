# Core dependencies — all modules
pydantic>=2.0
opencv-python>=4.9
ultralytics>=8.0        # YOLO v8/v11

# Tracking
# bytetrack is included in ultralytics; deepsort via:
# deep-sort-realtime>=1.3

# ANPR OCR (choose one — both pretrained, no custom training)
paddleocr>=2.7
# easyocr>=1.7

# Alarm Manager / API
fastapi>=0.111
uvicorn[standard]>=0.29
redis>=5.0
sqlalchemy>=2.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# Code quality (enforced by pre-commit)
black>=24.0
ruff>=0.4

# Utilities
pyyaml>=6.0
python-dotenv>=1.0
